# Static site example
At this moment, renders every `src/json/*.json` with template `src/html/product.html` into `dist`.

Requires Node 6.x

## How to install
`npm install`

## How to build
`npm run build`
